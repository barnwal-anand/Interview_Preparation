/* join() blocks the execution of thread that calls join, until launched thread execution is completed. In a way it acts
 * as a synchronization point.
 * detach() separates the launched thread from calling thread so that both threads can execute independently.
 */
